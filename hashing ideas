import java.security.MessageDigest;

String message1 = "hello";
    MessageDigest messageDigest = MessageDigest.getInstance("SHA-256");
    messageDigest.update(message1.getBytes());
    String stringHash = new String(messageDigest.digest());
nice!!
its a good idea if we make a method which use MessegeDigest class
but we don't need to convert the byte array to String, its more usefull to deal with byte array
private byte[] hash(String s) thrwos Exception{
    String message1 = "hello";
    MessageDigest messageDigest = MessageDigest.getInstance("SHA-256");
    messageDigest.update(message1.getBytes());
    return messageDigest.digest();
}
